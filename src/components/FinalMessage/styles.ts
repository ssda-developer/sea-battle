import styled from 'styled-components/macro';

export const StyledFinalMessage = styled.div`
    text-align: center;
`;

export const StyledFinalMessageTitle = styled.h2`
    margin: 0;

    svg {
        display: block;
        fill: var(--color-white);
        height: 250px;
        margin: 20px auto 0;
        width: 250px;
    }
`;
